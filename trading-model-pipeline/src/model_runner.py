
def train_and_evaluate(model_config, dataset):
    """Trains and evaluates the model using the provided dataset.

    Args:
        model_config -- An object containing all details about which model to use
            and how to train and evaluate it.
        dataset -- An object that provides information on how to retrieve the dataset.

    Returns:
        obj: An object containing details about the outcome of training and evaluation.
    """
    print("training and evaluating")
    return {result: "good"}
