task buildIt(dependsOn: [":data-model:buildIt", ":price-data-store:buildIt", ":backtester:buildIt", ":option-optimizer:buildIt", ":pattern-searcher:buildIt"]) << {
    println "- build visualization"
}

task gruntBuild(type:Exec) {
    commandLine 'grunt', 'build-test'
}

buildIt.dependsOn gruntBuild

task runIt << {
    println "- running visualization"
}

hello.doLast {
    println "- I handle visualization."
}
