package com.cowsunday.transforms

class RunCount extends Transform {
  override def transform(dataset: Array[Double]): Array[Double] = {
    var transformed = new Array[Double](dataset.length)

    var counter = -1
    dataset.foreach {

    }
  }
}
